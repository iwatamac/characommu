<link href="assets/characters_new.scss" rel="stylesheet">
<%= render "shared/header"%>

<div class="main-new">
  <div class="character-new">
    <div class="charaform-new">      
    <%= form_with model: @place, local: true do |f| %>

    <div class="first">
    <%= image_tag "place_1.png",class:"place_1" %>
    </div>
    <div class="second">
    <%= image_tag "place_2.png",class:"place_1 _2" %>
    </div>
    <div class="third">
    <%= image_tag "place_3.png",class:"place_1 _3" %>
    </div>
    
    <div class="force">
    <%= image_tag "basyo_sakusei.jpg", class:"form-header-text form-header-text_image mocchiri" %>
    </div>
    <p></p>
    <%= render 'shared/error_messages', model: @place %>

  <div class="form-group">
    <div class="nickname-form">
      <label for="名前" class="sr-only">名前</label>
      <%= f.text_area :name, class:"form-control form-control-newchara slidein", id:"character-name", placeholder:"場所の名前を入力してください" %>
    <p></p> 
    </div>
      <label for="自分のキャラ" class="sr-only">性別</label>
      <select name="place[character_ids][]" class="form-control slidein">
          <option value="">チャットする自分のキャラクターを選択してください</option>
          <% Character.where(user_id: current_user.id).each do |character| %>
            <option value=<%= character.id %>><%= character.name %></option>
          <% end %>
        </select>
        <p></p>
        <select name="place[character_ids][]" class="form-control slidein">
          <option value="">チャットするキャラクターを選択してください</option>
          <% Character.where.not(user_id: current_user.id).each do |character| %>
            <option value=<%= character.id %>><%= character.name %></option>
          <% end %>
        </select>
        <div class="">
        <%= image_submit_tag "sakusei_btn", class:"sakusei_btn", id:"sakusei_basyo" %>
        </div>
        <% end %>
      </div>
    </div>
  </div>




